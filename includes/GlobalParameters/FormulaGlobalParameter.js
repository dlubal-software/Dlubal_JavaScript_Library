function FormulaGlobalParameter(name,
                                symbol,
                                unit_group,
                                formula,
                                comment)
{
    engine.create_formula_global_parameter(name, symbol, unit_group, formula, comment);
}
