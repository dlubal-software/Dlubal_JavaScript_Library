function ValueGlobalParameter(name,
                              symbol,
                              unit_group,
                              value,
                              comment)
{
    block.create_value_global_parameter(name, symbol, unit_group, value, comment);
}
